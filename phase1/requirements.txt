# requirements.txt
# NOTE: install PyTorch separately using the official instructions for your CUDA version:
#   https://pytorch.org/get-started/locally/
# Example (CPU-only): pip install torch torchvision --extra-index-url https://download.pytorch.org/whl/cpu

torch>=1.12.0        # pick the correct build for your CUDA; see PyTorch site
ultralytics>=8.0.0
opencv-python
numpy
pyyaml
pillow
h5py
torchvision
